# 更新日志

## 1.2.1 - 2026-09-10

同步上游最新修复：official 模式按「几月几日」匹配、指令回复统一为分页图片，
并修复多池天井与任务配额的提示问题。版本号使用 1.2.1 提交审核。

### 卡池与 official 模式

- official 模式改为按「几月几日」匹配历史官方池（忽略年份）：无法获取最新排期时，
  按往期同月同日的排期模拟；跨年池按年末尾段 + 年初头段处理，结束日仍按不含当天计算。
- `/卡池 列表` 只输出 UP 角色信息图片（池名 + 全部 UP SSR + ID），不再附带
  类型/时间/天井等详情与卡池公告图；`/卡池` 概览仍会发送各启用池的官方公告图。
- 修复 `/卡池 下一期` 重复显示类型/时间的问题；`/卡池` 增加当天日期与 UP 卡总数，
  多池同时启用时按池分别显示天井进度。

### 指令输出与排版

- 全部指令回复统一渲染为分页图片卡片，渲染或发送失败时自动回退合并转发/纯文本；
  长文本按单页高度自动分页，并在标题与页脚标注页码。
- 统一卡片排版与文案：字段使用全角「｜」分隔、超长卡池名/卡名截断加省略号，
  签到、天井兑换等提示语改为更清晰的短句。
- 修复指定未启用卡池抽卡时的提示：现在明确返回「指定卡池 X 当前未启用」并列出
  当前启用池，而不是笼统提示「当前没有可用卡池」。
- 修复天井指令的已兑换状态提示：`/天井 <卡ID>` 只列出仍可兑换的池并附注已兑换池，
  全部兑换后明确提示已无可兑换池；`/天井池` 对未启用的池 ID 改为提示
  「卡池 X 当前未启用」并列出当前启用池。

### 任务与维护

- 修复每日任务配额用尽时的提示混用内部英文标识：现在显示
  「今日普通/挑战任务次数已用完」。
- 同步上游的临时图片清理范围（抽卡、文本卡片、签到彩蛋卡、任务卡）。

### AstrBot 适配

- 保留内部 ID 与数字 QQ 绑定（`/绑定QQ`）、openid 友好显示与管理员别名解析。

## 1.2.0 - 2026-09-10

同步上游 v1.2.0 / v1.2.1 的全部内容：新增音游随机任务系统，并补齐挑战/终极
任务难度、每日维护与历史清理修复。

### 随机任务

- 接入音击、舞萌 DX、中二节奏三游戏真实曲库，统一归一化难度标识、标级、定数与曲绘地址。
- 普通任务任意难度；挑战任务随机选择至少有一张 10 级或以上谱面的歌曲，并选取该曲
  达到门槛的最低真实谱面（例如舞萌 DX 的 `EXPERT DX 10`），要求该谱面或以上 S 评级，
  不会强制锁定曲目最高难度；终极任务从谱面定数 ≥ 14.7（内部定数阈值，不是“14 级+”）
  的超高难谱面随机，要求 SSS+。
- 接取任务时发送柔和渐变任务卡，包含任务 ID、曲目、谱面、要求、奖励和接取人；
  曲绘或任务卡生成失败时自动回退文字模式，任务信息始终完整。
- 终极任务要求只保留目标谱面与评级（如 `MASTER 15+ · SSS+ 评级`）；
  任务卡超长曲名与艺术家会按可用宽度截断并追加省略号。
- 普通/挑战任务按每日次数限制，未完成与已拒绝任务在每日 00:00（UTC）自动过期，
  已提交待审核任务始终保留；终极任务同一时间只能有一个未完成或待审核，
  完成后该曲从用户个人池永久移除，管理员确认后才能接取下一个。
- 管理员人工审核并发放点数：普通 20；挑战 S 30、SS 40、SSS/SSS+ 50；终极 30000。
- 新增隐藏管理员 `/任务重置 <任务ID> [备注]`，用于处理无法游玩对应游戏等情况；
  审核与重置均写入 `task_audit` 审计日志。
- 新增 `/任务清理 [天数]` 管理员命令，可立即清理已审核、已过期、已重置的历史任务；
  配合 `auto_cleanup_history` 与 `task_history_retention_days`（默认开启、保留 30 天）
  避免任务列表长期膨胀，待审核任务和审计日志不受影响。
- 曲绘下载优先使用系统 `curl` 绕过服务端 WAF，失败时回退 Python 下载，并保留
  `!webp` / `assets2` 多 URL 兜底、重试与本地缓存复用。
- 修复挑战任务接取时奖励显示为 0 的问题，默认按 S 档 30 点。

### 卡池、经济与维护

- 插件每次加载和每日维护时按真实日期（UTC）同步周 5 连保底、囤点周期并处理跨日任务，
  时间状态持久化，不依赖插件连续运行时长；日期切换统一改为国际时间 UTC。
- 囤点档位奖励默认每 60 天重置，可在 `[economy]` 调整。
- 卡池轮替改为持久化起始日，重启后直接进入正确轮替位置，不再重新计算 15 天周期。
- official 模式支持同一天多个官方卡池同时启用：`/卡池` 列出全部启用池，默认抽取
  最近开启的池，`/抽卡 <池ID> <数量>` 可指定其他池，`/天井列表` 列出全部启用池状态。
- 修复抽卡落地失败时回滚半价/周保底状态可能抛 `NameError` 的问题。
- 签到获得彩蛋卡时会同步渲染并发送该卡的卡面图，失败时自动回退文字提示。

### 其他

- 任务提交的“必须附带成绩照片”检查现在能识别消息中的图片组件。
- `/终极完成` 支持 `<@用户>` 形式与数字 QQ 别名解析。
- 配置面板新增「随机任务」分组，更新 README、USAGE、规则文案与帮助信息。
- 任务卡底部标注改为「音击抽卡模拟器 · 随机任务」；接取人 ID 过长时自动省略，
  不再与备注、品牌文字重叠。
- 在以 openid 识别用户的平台（如 QQ 官方机器人）上，接取、查看或提交任务前
  需要先使用 `/绑定QQ <QQ号>`；任务卡与审核列表展示绑定的数字 QQ。
- QQ 官方机器人不支持合并转发，长内容改用 Markdown 渲染，发送失败时自动
  回退为纯文本。
- 规则、卡池、概率、天井列表、卡册、任务列表等较长内容改为渲染为浅色渐变
  图片卡片；超过单页高度时自动分页，页脚标注页码，渲染失败时回退转发或纯文本。

## 1.1.0 - 2026-09-05

- 支持 QQ 官方内部用户 ID（openid）与数字 QQ 号混用时的身份合并。
- 新增用户身份别名表与 `/绑定QQ`：绑定后可统一合并同一玩家的数字 QQ 和 openid 档案。
- `/奖励` 和管理员白名单支持数字 QQ 别名；未绑定的数字 QQ 不再创建重复档案。

新增官方卡池排表、卡内权重、池子轮替、连续签到、月卡和管理员发放点数功能，并统一整体产品名为“音击抽卡模拟器”。

- 修正历史卡池候选模型：活动池不再只包含公告列出的新卡/选择卡，而是按活动日期混入当时已有全部 R/SR/SSR，再叠加本期 UP 与天井选择卡；常驻池同步扩为当前版本全部基础卡。
- 抽卡结果图底部按 RinNET 样式叠加卡牌数字 ID 与完整卡号；`/卡图` 保持发送原始高清卡面。
- 获取 SEGA CARDMAKER 官方公告中的 63 个历史卡池，覆盖 2020-10～2026-07。
- 导入 Artemis `static_gachas.csv` / `static_gacha_cards.csv` 的卡内权重、UP 与选择标记。
- 支持 `cycle` 历史池轮替（默认每 15 天换池）和 `official` 官方日期选池两种模式。
- 抽卡改为“稀有度权重 → 池内卡权重 → UP 倍率”，UP 卡默认 ×10。
- 新增 `/卡池` 命令，可查看本期池、UP 数量、天井点数和轮替进度。
- `/抽卡` 和 `/概率` 现在显示当前卡池与 UP 信息。
- `/卡池` 会获取 63 期官方公告主图并随文字一起发送，图片本地缓存 7 天。
- 新增常驻池：包含当前版本已有的全部 R/SR/SSR 基础卡，可通过 `/抽卡 常驻` 单独抽取；`/抽卡` 默认仍抽取当前轮替活动池。
- 非抽卡来源卡从所有抽卡池中剔除，改为每日签到按概率随机获得；排表每条卡记录附带 `version` 和 `cardNumber` 用于复盘 ID。
- 落地天井机制：抽卡按池累计天井点，满点后可通过 `/天井 <卡ID>` 兑换当前池选择卡；每个池独立状态且只能兑换一次。
- 优化指令文案：精简 `/帮助`，新增 `/规则` 转发详细说明；长文本和可选卡列表自动使用转发消息发送。
- `/卡池` 增加当前池 UP SSR 角色与 ID 展示，`/卡池 列表` 可查看完整 UP SSR 列表。
- 明确无 UP 池的文案：`UP 卡：0 张` 只表示官方公告没有概率提升名单，抽卡候选仍为当期版本已有全部 R/SR/SSR；新增独立 `/天井列表` 入口，可查看当前池全部天井选择卡的 ID 与角色。
- `allow_local_operator` 默认关闭，管理员发放默认只允许 QQ 白名单。

- 连续签到每天递增奖励，最高每日额外 100 点；断签重新计算。
- 连续签到第 7 天、14 天、21 天等额外获得 500 点。
- 新增月卡：默认 1500 点购买 30 天，每日签到额外 100 点，剩余不超过 3 天可续费，购买后获得两次半价五连。
- 新增管理员配置和 `/奖励 <QQ号> <点数> [备注]`，并支持通过 `@用户` 直接指定目标，发放记录写入审计日志。
- 概率、卡牌信息、素材目录和新增配置支持 WebUI 热更新，不再需要重启插件。
- 重新平衡奖励：日常签到 100～250 点，新增 15 天卡池周期奖励 1000 点；新增 1000/3000/10000 点囤点档位奖励 100/500/2000 点，周奖励、月卡加成和半价次数同步调高，长期签到和囤点抽卡收益更明显。
- 清理开发期内部叫法，用户可见文案统一为“音击抽卡模拟器”。

## 1.0.0 - 2026-09-04

首个公开发布版本。

### 抽卡

- 支持 1 连、5 连、11 连抽取。
- 采用两阶段抽取：先按稀有度权重，再在对应稀有度内抽取具体卡牌。
- 5 连每用户每周首次提供 SR 或以上保底，周四 07:00 重置。
- 11 连始终提供 SR 或以上保底。
- 支持以 QQ 用户为单位的独立点数、签到、库存和抽卡日志。

### 收藏与展示

- 支持通过命令查看卡册进度。
- 支持 `/卡图 <ID>` 查看已拥有卡牌的高清原图。
- 抽卡结果图支持 1 连、5 连和 11 连构图。
- 展示星级、MAX 标记、解花和超解花状态。

### 数据与部署

- 插件默认读取 `assets/card_data/` 中的卡牌 JSON 和卡面素材。
- 配置文件允许手动切换卡牌信息与卡面目录。
- 提供 `sync_card_data.py` 同步和 SHA-256 校验工具。
- 抽卡临时结果图保留 24 小时并自动清理。
- 每日签到包含当前配置的基础随机奖励。
- 新增 `metadata.yaml`、`_conf_schema.json`、`main.py` 和本地
  `maibot_sdk.py` 兼容层。
- 运行数据保存到插件数据目录，运行期不再写入插件目录；抽卡临时图保存在
  插件私有 `runtime/` 目录。
- 配置由 `_conf_schema.json` 在 WebUI 生成面板，覆盖素材路径、概率、
  经济值、月卡与管理员设置。

### 文档与许可

- README 改为简短产品介绍。
- 新增使用说明和详细免责声明。
- 代码采用 AGPL-3.0-or-later；卡面素材权利归原权利人所有。
